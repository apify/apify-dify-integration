identity:
  name: "apify-integration"
  author: "apify"
  label:
    en_US: "apify-integration"
    zh_Hans: "apify-integration"
    pt_BR: "apify-integration"
description:
  human:
    en_US: "A plugin to run Apify actors and tasks, and manage Apify resources directly from Dify."
    zh_Hans: "A plugin to run Apify actors and tasks, and manage Apify resources directly from Dify."
    pt_BR: "A plugin to run Apify actors and tasks, and manage Apify resources directly from Dify."
  llm: "A plugin to run Apify actors and tasks, and manage Apify resources directly from Dify."
parameters:
  - name: query
    type: string
    required: true
    label:
      en_US: Query string
      zh_Hans: 查询语句
      pt_BR: Query string
    human_description:
      en_US: "A plugin to run Apify actors and tasks, and manage Apify resources directly from Dify."
      zh_Hans: "A plugin to run Apify actors and tasks, and manage Apify resources directly from Dify."
      pt_BR: "A plugin to run Apify actors and tasks, and manage Apify resources directly from Dify."
    llm_description: "A plugin to run Apify actors and tasks, and manage Apify resources directly from Dify."
    form: llm
extra:
  python:
    source: tools/apify-integration.py
